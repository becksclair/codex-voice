export function App() {
  return (
    <main className="mx-auto flex min-h-dvh max-w-xl flex-col items-center justify-center gap-4 px-6 text-center">
      <h1 className="text-3xl font-semibold">Codex Voice</h1>
      <p className="text-balance opacity-70">
        The React shell port is in progress. This standalone frontend scaffold will replace the
        embedded PWA in a later phase.
      </p>
    </main>
  );
}
